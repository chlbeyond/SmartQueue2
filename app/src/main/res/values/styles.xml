<resources>

    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

    </style>

    <!--//启动页-->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@mipmap/new_login_bg</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="windowActionBar">false</item>
        <!--//一定要，否则有状态栏显示，不能全屏-->
        <item name="windowNoTitle">true</item>
    </style>


    <!--<item name="android:textSize">@dimen/listview_item_call_text</item>-->
    <!--<item name="apTabPaddingLeftRight">75px</item>-->
    <style name="pagertab_style">
        <item name="android:textSize">40px</item>
        <item name="apTabPaddingLeftRight">20px</item>
        <item name="apTabDrawMode">text</item>
        <item name="apTabShouldExpand">true</item>
        <item name="android:textColor">@color/textnormal</item>
        <item name="apTabIndicatorColor">@color/tab_selector</item>
        <item name="apTabTextSelectColor">@color/tab_selector</item>
    </style>

    <style name="pagertabport_style">
        <item name="android:textSize">40px</item>
        <item name="apTabPaddingLeftRight">30px</item>
        <item name="apTabDrawMode">text</item>
        <item name="apTabShouldExpand">true</item>
        <item name="android:textColor">@color/textnormal</item>
        <item name="apTabIndicatorColor">@color/tab_selector</item>
        <item name="apTabUnderlineColor">#1A000000</item>
        <item name="apTabTextSelectColor">@color/tab_selector</item>
    </style>

    <style name="dialogWindowAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>

    <style name="dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!--边框-->
        <item name="android:windowIsFloating">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">false</item>
        <!--半透明-->
        <item name="android:windowNoTitle">true</item>
        <!--无标题-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--背景透明-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--模糊-->
    </style>

</resources>
